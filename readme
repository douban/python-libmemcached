1.It needs libmemcached >=1.0.2 (touch support), patched with fail_over.patch, empty_string.patch, buffer_request.patch
2.If you use the python whose version is < 2.5, you should modify the Py_ssize_t definition.
